{
  "name": "backend",
  "version": "0.0.1",
  "description": "backend to introdemo notes app",
  "main": "index.js",
  "scripts": {
    "build:ui": "rm -rf dist && cd ../introdemo/ && npm run build && cp -r dist ../introdemo_backend",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push origin notes",
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \\\"Error: no test specified\\\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "none"
  },
  "keywords": [
    "none"
  ],
  "author": "ecg",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "serverless-http": "^3.2.0"
  }
}
